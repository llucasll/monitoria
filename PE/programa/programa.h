#include <stdbool.h>

#include "../data.h"

// Imprime o Menu de opcoes
void menu(void);

// Executa a acao selecionada
bool exec(short op);

// Escreve um elemento na tela
void escrever(Elemento e);
